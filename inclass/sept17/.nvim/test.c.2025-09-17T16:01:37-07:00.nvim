#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main() {
  if (fork() != 0) {
    printf("parent\n");
    execl("/bin/ls", "/bin/ls", "../../", "-a", NULL);
  } else {
    sleep(3);
    printf("child\n");
    execl("/bin/ls", "/bin/ls", "../../", "-alh", NULL);
  }
  return 0;
};
