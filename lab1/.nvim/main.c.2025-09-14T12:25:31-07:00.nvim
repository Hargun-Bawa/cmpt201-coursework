#include <stdio.h>
#include <stdlib.h>

int main() {
  char *line = NULL;
  size_t readd;
  size_t len = 32;
  readd = (char *)malloc(32 * sizeof(char));
  printf("%s", line);
  free(line);

  return 0;
};
