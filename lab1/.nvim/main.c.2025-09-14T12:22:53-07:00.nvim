#include <stdio.h>
#include <stdlib.h>

int main() {
  FILE *stream;
  char *line = NULL;
  size_t readd;
  size_t len = 0;
  readd = getline(&line, &len, stream);
  printf("%s", line);
  free(line);

  return 0;
};
