#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main() {
  int x = 0;
  waitpid(fork(), x, 0);
  printf("parent\n");
  execl("/bin/ls", "/bin/ls", "../../", "-a", NULL);
  sleep(3);
  printf("child\n");
  execl("/bin/ls", "/bin/ls", "../../", "-alh", NULL);
  return 0;
};
